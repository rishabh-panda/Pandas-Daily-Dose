#include<stdio.h>
    int main() {
    
    //number to generate the mathematical table
    int n=10;
        
    //loop for iteration
    for(int i=n; i>=1; i--)
    
    //printing the table values    
    printf("%d X %d = %d\n",n,i,n*i);
 
    return 0;
}
